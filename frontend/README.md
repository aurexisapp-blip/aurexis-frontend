# StackIQ Local Frontend

Minimal Vite + React frontend that consumes existing backend endpoints:

- `GET /best_pick_v2`
- `GET /analyze/{symbol}`

## Run locally

```bash
cd frontend
npm install
npm run dev
```
